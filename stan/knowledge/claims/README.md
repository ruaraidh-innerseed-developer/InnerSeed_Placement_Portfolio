# Claims

One statement, one source, one verbatim quote. Schema in `../schema/claim.schema.yaml`.

**Empty, and correctly so.** A claim cannot be extracted from a document nobody
has read, and no source in `evidence/sources/` yet has `full_text_read: true`.

This folder fills once documents arrive in `sources-inbox/`. See `../../BACKEND-PLAN.md`.
